#include <bits/stdc++.h>
using namespace std;
#define loop(i,a,n) for(int i = a; i < n; i++)
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0); cout.tie(0);
    cin >> n;
    loop(i,0,n){
        cin >> temp;
        a.push_back(temp);
        b.push_back(temp);
    }
    sort(b.begin(),b.end());
    for()
}