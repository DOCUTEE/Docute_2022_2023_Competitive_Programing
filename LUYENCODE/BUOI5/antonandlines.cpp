#include <bits/stdc++.h>
using namespace std;
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0); cout.tie(0);
    cin >> n; 
    cin >> x1 >> x2;
    for (int i = 0; i < n; i++){
        cin >> k[i] >> b[i];
        pos1[i] = k[i] * x1 + b[i];
        pos2[i] = k[i] * x2 + b[i];
    }
    int max = pos1[]
}