#include <bits/stdc++.h>
using namespace std;
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    cin >> t;
    while (t--){
        cin >> n >> k;
        if (n % 2 == 1) sumodd = ((n+1)/2)*((n-1)/2+1);
        else sumodd = ((n/2)*((n-2)/2+1);
        
    }
}